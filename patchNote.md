## [2026-09-12] 넓은 모니터에 최적화된 좌우 2열 대시보드 및 카드 레이아웃 개편

### 주요 변경 사항

#### 1. 데스크탑 화면 크기별 반응형 기준 정돈 (`global.css`)
- **화면 크기별 맞춤 기준 확립**:
  - 창을 작게 줄였을 때(`1024px` 미만): 화면이 잘리지 않도록 위아래 1열로 자동 정렬하고 세로 스크롤 지원.
  - 기본 데스크탑(`1024px~`): 왼쪽(시계/날씨)과 오른쪽(캐러셀)이 나란히 배치되는 좌우 2열 대시보드 진입.
  - 13~14인치 노트북(`1336px`), 일반 모니터(`1440px`), 대형 화면(`1920px~` FHD/QHD/4K)에 맞춰 최적의 간격과 크기 제공.
- **반응형 충돌 스타일 정리**: 화면 크기가 달라질 때 카드 배치가 꼬이거나 어긋나던 스타일 코드를 체계적으로 정돈.

#### 2. 메인 대시보드 좌우 2열 배치 개편 (`MainSec.tsx`, `MainContainer.tsx`)
- **시원한 좌우 2열 구성**: 데스크탑 화면에서 왼쪽에는 시계와 날씨 카드, 오른쪽에는 할 일·메모·뉴스 카드를 양옆으로 나란히 배치해 넓은 모니터 공간을 알차게 활용.
- **창 크기를 줄였을 때의 화면 보호**: 창을 작게 줄이거나 화면을 반으로 분할했을 때는 위아래 1열로 매끄럽게 바뀌며, 내용이 잘리지 않고 아래로 스크롤할 수 있도록 개선.

#### 3. 좌측 시계/날씨 세로 2단 카드 분리 (`Container.tsx`, `Today.tsx`, `WeatherView.tsx`, `WeatherChart.tsx`)
- **독립된 2단 글래스 카드 구성**: 기존에 한 상자에 뭉쳐 있던 구조를 분리하여 상단 시계/날짜 카드(`h-[160px]`)와 하단 날씨 카드(`h-[296px]`)의 2단 세로 카드로 재구성.
- **높이 균형 및 그래프 곡선 잘림 해결**: 기온 그래프 꼭대기가 잘리지 않도록 위쪽 여백(`padding.top: 12`)을 넓히고, 날씨 카드 높이를 확장해 우측 캐러셀 카드와 완벽한 높이 균형을 맞춤.

#### 4. 우측 캘린더 슬라이드 전폭 단일 카드화 (`Carousel`, `CalendarSlide`, `NewsBrief`, `CoinList`)
- **넓고 시원한 단일 카드 구조 (`CalendarSlide`)**: 기존에 좌우로 비좁게 나뉘어 있던 구조(1/3 날짜 선택 vs 2/3 일정 목록)를 시원한 단일 카드 구조로 개편.
  - 상단: 할 일/메모 탭 버튼, 개수 배지, 날짜 이동 버튼(`< 9월 12일 >`) 배치.
  - 중앙: 카드 전체 너비를 꽉 채우는 넓고 쾌적한 일정/메모 목록(`CalendarList`).
  - 하단: 연필 아이콘, 글자 수 세기, 마감일 달력, 추가 버튼이 하나로 묶인 하단 도킹 입력창(`ItemInput`).
- **뉴스/코인 카드 비율 최적화**: 새로 정돈된 카드 높이에 맞춰 AI 뉴스 브리핑과 실시간 코인 시세 화면의 여백과 스크롤 경험 개선.

#### 5. 상단 검색창 너비 정렬 및 일체감 확보 (`Search.tsx`)
- **대시보드 너비와 완벽 일치**: 좌우 2열 대시보드 전체 가로폭(약 894~924px)에 딱 맞도록 검색창 길이를 정렬하여 한눈에 보기에 안정적인 비례 제공.
- **은은한 디자인과 친절한 안내 문구**: 은은한 반투명 글래스 배경과 부드러운 포커스 효과, 직관적인 안내 문구("무엇을 검색하시나요?")를 적용.

#### 6. 사이드바 열기/닫기 아이콘 개선 (`BookmarkBar.tsx`)
- **직관적인 화살표 아이콘 교체**: 사이드바가 열렸을 때는 닫는 방향인 왼쪽 화살표(`<`), 닫혔을 때는 여는 방향인 오른쪽 화살표(`>`)로 교체하여 개폐 상태를 한눈에 알아볼 수 있도록 개선.

#### 7. 날씨 카드 상하(위아래) 슬라이드 및 마우스/터치 제스처 지원 (`WeatherView.tsx`)
- **위아래로 넘겨보는 상하 슬라이드**: 2열 대시보드 구조에 맞춰 기존 좌우 넘기기 대신 위아래로 부드럽게 넘겨볼 수 있도록 전환.
- **마우스/터치 수직 드래그(스와이프) 지원**: 마우스나 손가락으로 슥 밀어서 '현재 날씨'와 '3일 예보'를 직관적으로 넘겨볼 수 있도록 제스처 기능 탑재.
- **우측 세로형 도트 버튼 개편**: 상하 슬라이드 방향에 맞춰 오른쪽에 세로형 알약 도트 버튼을 배치하여 현재 페이지를 확인하고 클릭해서 전환할 수 있도록 지원.
- **드래그-클릭 간섭 방지**: 마우스로 화면을 드래그할 때 실수로 버튼이 눌리지 않도록 방어 로직 적용.

#### 8. 날씨 카드 로딩 중 깜빡임 방지 (스켈레톤 UI 도입, `WeatherSkeleton.tsx`)
- **부드러운 로딩 경험**: 날씨 정보를 불러오는 동안 단순 텍스트("로딩 중...")가 뜨다가 화면이 덜컹거리는 현상을 막기 위해, 실제 날씨 카드와 크기가 똑같은 은은하게 반짝이는 로딩 틀(스켈레톤 UI)을 표시.

#### 9. 3일 단기 예보 날씨 아이콘 시각화 (`WeatherIcon.tsx`, `WeatherView.tsx`)
- **직관적인 날씨 아이콘 추가**: 3일 예보 카드(오늘/내일/모레)에 글자뿐만 아니라 예쁜 원형 날씨 아이콘(맑음, 구름, 비 등)을 넣어 한눈에 날씨를 알아볼 수 있도록 개선.
- **다크 모드 가시성 지원**: 어두운 화면(다크 모드)에서도 아이콘이 잘 보이도록 색상 자동 반전 지원.

#### 10. 메모/할 일/뉴스 목록 스크롤 시 바깥 슬라이드 간섭 차단 (`CalendarList.tsx`, `NewsBriefResult.tsx`)
- **목록 스크롤 영역 독립 분리**: 메모/할 일 목록이나 뉴스 결과창 안에서 마우스 휠을 굴릴 때, 바깥쪽 큰 슬라이드가 함께 넘어가지 않고 목록 내용만 자연스럽게 스크롤되도록 분리.
- **깔끔한 5px 슬림 스크롤바 적용**: 답답하게 숨겨져 있던 스크롤바를 얇고 세련된 반투명 스크롤바로 변경해 현재 스크롤 위치를 쉽게 파악하고 마우스로 잡고 내릴 수 있도록 개선.

#### 11. 확장 프로그램 성능 검사(Lighthouse) 지원 (`manifest.json`)
- **성능 측정 도구 지원**: Lighthouse 등 웹 성능 진단 도구가 확장 프로그램 리소스에 원활히 접근할 수 있도록 설정 보강.

#### 12. 외부 웹폰트 로컬 탑재로 첫 화면 열리는 속도 개선 (`global.css`, `public/fonts/`)
- **외부 인터넷 글꼴 로컬화**: 외부 웹 서버에서 글꼴을 매번 다운로드받던 방식을 확장 프로그램 내부 글꼴 파일(Paperlogy WOFF2)로 전환하여 첫 화면이 깜빡임 없이 즉각 뜨도록 최적화.

---

## [2026-09-12] 크롬 확장 프로그램 맞춤설정 세션 영역 확보 및 전반적인 세로 레이아웃 축소

## [2026-09-12] 크롬 브라우저 맞춤설정 버튼 영역 확보 및 컴팩트한 화면 여백 최적화

### 주요 변경 사항

#### 1. 메인 화면 상하 여백 압축 (`MainSec.tsx`)
- **화면 수직 중앙 정렬**: 상단에 과도하게 들어가 있던 여백을 제거하여 전체 화면이 모니터 정중앙에 편안하게 정렬되도록 조정.
- **크롬 기본 버튼과의 겹침 방지**: 검색창과 카드 사이 간격을 줄이고, 하단에 안전 여백(`pb-10`)을 두어 크롬 브라우저 우측 하단의 '맞춤설정' 톱니바퀴 버튼과 대시보드가 겹치는 현상을 원천 방지.

#### 2. 검색창 세로 높이 슬림화 (`Search.tsx`)
- **컴팩트한 크기**: 검색창 세로 높이를 `60px`에서 `48px`로 줄여 화면을 덜 차지하도록 다이어트.
- **글자 및 아이콘 균형**: 줄어든 높이에 맞춰 글자 크기와 돋보기 아이콘 비율을 조화롭게 재배치.

#### 3. 시계 및 날씨 카드 세로 높이 최적화 (`Container.tsx`, `Today.tsx`, `WeatherView.tsx`)
- **불필요한 안쪽 여백 축소**: 시계 영역과 날씨 카드의 위아래 패딩을 줄여 답답하지 않으면서도 컴팩트하게 정돈.
- **시계 글자 크기 조정**: 디지털 시계 글자 크기를 `72px`에서 `60px`로 알맞게 줄여 전체 화면과의 시각적 균형 유지.

#### 4. 캐러셀 슬라이드 높이 다이어트 (`Carousel`, `CalendarSlide`, `NewsBrief`, `CoinList`)
- **높이 통일**: 지나치게 높았던 캐러셀 카드 높이를 슬림하게 줄이고, 각 슬라이드(일정/뉴스/코인) 내부 여백을 통일감 있게 정돈.

---

## [2026-09-12] Todo 마감일(Deadline) 표시 및 마감 임박 붉은 강조 알림 구현

### 주요 변경 사항

#### 1. 마감일 계산 및 디데이(D-Day) 판별 로직 추가 (`todoDate.ts`)
- **정확한 날짜 계산**: 브라우저나 컴퓨터 시간 설정에 따른 날짜 계산 오차를 막고, 당일(`D-Day`), 하루 전(`D-1`), 기한 지남(`D+N`)을 정확히 계산하는 날짜 유틸리티 구현.
- **마감 임박 기준 마련**: 마감일 당일, 하루 전, 기한이 지난 미완료 할 일은 긴급 항목으로 자동 분류 (단, 체크 완료된 할 일은 제외).

#### 2. 마감 임박 항목 붉은색 경고 강조 UI 적용 (`ItemList.tsx`, `CalendarList.tsx`)
- **직관적인 마감일 뱃지**: 마감일이 있는 할 일에 달력 아이콘과 함께 `D-Day`, `D-1`, 날짜 뱃지를 표시.
- **눈에 띄는 붉은색 경고 배경**: 마감 임박 및 기한이 지난 미완료 할 일은 은은한 붉은빛 글래스 배경으로 강조하여 중요한 일정을 놓치지 않도록 유도.
- **완료 체크 시 강조 해제**: 할 일을 완료 체크하면 붉은 배경이 즉시 사라지고 차분한 완료(취소선) 스타일로 변경.

#### 3. 등록일부터 마감일까지 전 기간 달력 노출 및 기간 뱃지 개선
- **일정 기간 연속 노출**: 마감일이 있는 할 일이 당일에만 보이는 문제를 해결하고, 등록한 날부터 마감일까지 달력 날짜를 탐색할 때 계속해서 목록에 보이도록 개선.
- **기간 뱃지 표시**: 마감 날짜뿐만 아니라 `09.01 ~ 09.10` 형태의 전체 일정 기간을 한눈에 알아볼 수 있도록 뱃지 디자인 개선.

#### 4. 안정성 검증 테스트 보강
- 날짜 연산, 마감일 뱃지, 리스트 필터링 동작을 검증하는 34개의 단위/컴포넌트 테스트를 추가하여 총 75개 테스트 100% 통과 완료.

---

## [2026-09-11] 로그인 없이 사용할 때 데이터가 초기화되던 버그 해결 및 안전한 저장 보장 (v1.3.03)

### 주요 변경 사항

#### 1. 백엔드 통신용 임시 세션과 정식 로그인 상태 분리
- **껍데기 프로필 노출 차단**: 북마크 파비콘 저장 등 백엔드 통신을 위해 사용되던 임시 '익명 세션'이 실제 구글 로그인 계정으로 잘못 처리되어, 프로필에 빈 '사용자' 계정이 뜨던 오류를 원천 차단.
- **순수 게스트 모드 유지**: 실제 구글 로그인 유저만 정식 계정으로 인식하고, 비로그인 상태에서는 순수 손님(게스트) 모드를 유지하도록 명확히 분리.

#### 2. 안정적인 고정 게스트 저장소 보장으로 데이터 유실 방지
- **고정 저장 키 적용**: 새로고침할 때마다 매번 새로운 임시 아이디가 만들어져 기존 게스트 데이터를 찾지 못하고 덮어쓰던 문제를 해결하고, 항상 안전한 고정 저장 키(`guest_fav_items`, `guest_list_items`)를 사용하도록 통일.

#### 3. 게스트 ➔ 구글 로그인 시 작성한 데이터 자동 이어받기(마이그레이션)
- **작성한 데이터 그대로 보존**: 로그인 없이 게스트 상태로 북마크나 메모/할 일을 작성하다가 나중에 구글 계정으로 로그인하면, 기존 데이터가 새 구글 계정으로 자동으로 복사되어 데이터가 날아가지 않고 그대로 이어짐.

#### 4. 브라우저 종료 및 새로고침 시 즉시 저장 안전장치 (플러시 가드)
- **작성 중 창 닫힘 방어**: 할 일이나 메모를 작성하고 0.5초 이내에 바로 탭을 닫거나 새로고침해도 작성 중인 내용이 유실되지 않도록 브라우저 종료 시점에 즉시 강제 저장하도록 보강.

---

## [2026-09-09] 크로스 플랫폼 빌드 안정화 및 웹 데모 사이트 자동 배포(CI/CD) 구축

### 주요 변경 사항

#### 1. 크롬 확장 프로그램 팝업 파일 번들링 연결 정상화
- **팝업 파일 경로 정돈**: `public/` 폴더에 있던 `popup.html`을 프로젝트 루트로 옮기고 빌드 설정에 연결하여, 빌드 후 정상 스크립트가 누락 없이 연결되도록 수정.

#### 2. 윈도우/맥/리눅스 어디서나 동일하게 작동하는 빌드 명령 정규화
- **운영체제 전용 복사 명령 제거**: 윈도우 전용 복사 명령(`copy`, `xcopy`)을 제거하고 Vite 표준 빌드로 일원화하여 리눅스(GitHub Actions)와 맥, 윈도우 모두에서 동일하게 빌드되도록 개선.

#### 3. GitHub Pages 웹 데모 자동 배포 파이프라인 구축
- **깃허브 푸시 시 자동 배포**: 코드를 깃허브 메인 브랜치에 푸시하면 빌드부터 배포까지 자동으로 처리되어 [웹 데모 사이트](https://park-intae.github.io/doorframe/)에 즉시 반영되는 워크플로우 도입.

#### 4. 오프라인 날씨 아이콘 경로 수정
- 외부 인터넷 연결 없이도 로컬에 탑재된 날씨 아이콘들이 깨짐 없이 정상 출력되도록 상대경로 보정.

---

## [2026-09-09] 24시간 멈추지 않는 비상 백업 날씨망 연동 및 구글 로그인 정상화 (v1.3.02)

### 주요 변경 사항

#### 1. 날씨 서버 점검 시에도 멈추지 않는 비상 백업 날씨망 (Dual Fallback) 구축
- **정부 서버 점검 대비**: 기상청이나 국토교통부 서버 점검, 일시적 오류 발생 시 유럽중기예보센터 기반의 해외 무료 기상망(Open-Meteo)과 글로벌 지오코더로 자동 전환되어, 날씨 화면이 `0°`나 에러로 멈추지 않고 24시간 정상 작동하도록 개선.
- **지도 좌표 오류 해결**: 주소 변환 좌표계 기준을 표준화하여, 간혹 '알 수 없는 지역'으로 표시되던 위치 변환 버그를 완벽히 해결.

#### 2. 사용자 동선 일원화 및 캘린더 화면 내 입력창 통합
- **복잡한 메뉴 제거**: 우측 하단에 흩어져 있던 플로팅 버튼과 팝업 메뉴를 과감히 없애고, 캘린더 슬라이드 화면 안에서 날짜를 선택하고 메모나 할 일을 바로 입력할 수 있도록 UX를 깔끔하게 통합.

#### 3. 입력창 및 리스트 UI 다듬기
- **일체감 있는 디자인**: 입력창 배경을 리스트와 똑같은 은은한 반투명 글래스 테마로 맞추고, 어두운 테마(다크 모드)에서도 완벽하게 어울리도록 개선.
- **직관적인 아이콘 교체**: 삭제 버튼을 직관적인 휴지통(`Trash2`) 아이콘으로 교체하고 마우스 호버 시 붉은색 피드백을 주어 편리하게 삭제할 수 있도록 변경.

#### 4. 크롬 확장 프로그램 구글 로그인 차단 문제 해결
- **새 브라우저 탭 인증 방식 도입**: 구글 보안 정책으로 인해 확장 프로그램 내부 팝업에서 로그인이 차단(`400 Bad Request`)되던 문제를 해결하기 위해, 크롬 일반 새 탭을 띄워 안전하게 구글 로그인을 진행하고 인증이 끝나면 탭이 자동으로 닫히도록 개선.
- **로그인/로그아웃 아이콘 고화질 벡터화**: 깨지기 쉬운 이미지 파일 대신 선명한 벡터 아이콘으로 교체하여 어떤 해상도에서도 깔끔하게 보이도록 정리.

---

## [2026-09-09] 확장 프로그램 보안 강화 및 첫 배포 준비 (v1.3.01)

### 주요 변경 사항

#### 1. 외부 API 비밀키 서버 뒷단 격리 (보안 강화)
- **비밀키 노출 차단**: 기상청, Gemini AI, 지도 API 등 비용과 호출 한도가 걸린 중요한 비밀키가 사용자 PC의 확장 프로그램 소스 코드에 노출되지 않도록 서버 뒷단(Supabase Secrets)으로 안전하게 숨김.
- **클라이언트 공개 키 표준화**: 확장 프로그램에는 안전한 공개 키만 들어가도록 정리하여 해킹이나 키 탈취 위험을 사전에 차단.

#### 2. 캐러셀 슬라이드 컴팩트화
- **핵심 기능 집중**: 권한 요구가 많고 사용성이 떨어지던 유튜브 플레이어를 제외하고, 실생활에 유용한 핵심 3종(캘린더/메모, AI 뉴스 브리핑, 실시간 코인 시세)으로 슬라이드를 컴팩트하게 재편.

#### 3. 위치 권한 안전장치 및 코인 시세 연동 안정화
- **위치 거부 시 서울 기본 날씨 제공**: 위치 권한을 허용하지 않아도 서울 기준으로 날씨가 안전하게 표시되고 `[기본 위치]` 안내 뱃지가 뜨도록 안전장치 마련.
- **코인 시세 차단 해결**: 업비트 API 접근 권한을 정상 등록하여 실시간 코인 가격 조회가 막힘없이 작동하도록 조치.

---

## [2026-05-25] UI/UX 폴리싱 (내비게이션 및 팝업 최적화)
### 주요 변경 사항
#### 1. 내비게이션 Popover 최적화
- **너비 일관성 확보**: `Popover` 컴포넌트 내부의 하드코딩된 스타일을 제거하고, 부모 컴포넌트(`ActionPopover`)에서 명시적으로 너비를 제어할 수 있도록 구조 최적화 (기본 너비 500px 적용).
- **코드 정리**: 사용되지 않는 `popoverDiv` CSS 클래스 제거를 통해 코드베이스 유지보수성 향상.

#### 2. 하단 내비게이션 가시성 개선
- **모드별 아이콘 색상 최적화**: 라이트 모드에서는 아이콘 색상을 검은색으로 고정하고, 다크 모드에서는 흰색으로 반전되도록 CSS 필터(`brightness-0`, `dark:invert`, `dark:brightness-100`)를 적용하여 어떤 테마에서도 명확한 아이콘 가시성 확보.

---
## [2026-05-24] 메모/할 일 팝업 UI 디자인 개선
### 주요 변경 사항
#### 1. 팝업 레이아웃 계층 구조화 (Layout Re-architecture)
- **명확한 정보 계층**: 팝업 내부를 `Header`, `Input Area`, `List Area`로 명확히 분리하여 사용자가 정보 흐름을 직관적으로 인지하도록 구조 개선.
- **가시성 향상**: 입력창(`ItemInput`)을 기존 폼 내부에서 벗어나 개별 영역으로 분리하고, 팝업 너비를 400px(데스크탑 환경 최적화)로 확장하여 가독성 증대.

#### 2. 인터랙션 및 피드백 강화
- **포커스 글로우 효과**: 입력창 포커스 시 글래스모피즘 테마에 맞춘 글로우 효과(shadow)를 적용하여 입력 상태 강조.
- **버튼 피드백**: 추가 버튼(`PlusIcon`) 클릭 시 스케일 변환 애니메이션(hover/active)을 적용하여 경쾌한 피드백 구현.
- **마감일 상태 시각화**: 마감일이 설정된 경우 달력 아이콘 색상을 포인트 색상으로 변경하여 상태 식별 용이성 강화.

#### 3. 디자인 시스템 일관성 및 폴리싱
- **글래스모피즘 테마 통일**: 기존 인라인 스타일을 전면 제거하고 Tailwind 유틸리티 클래스 기반의 `glass` 스타일을 적용하여 일관된 시각적 테마 유지.
- **리스트 아이템 디자인**: 체크박스 및 삭제 버튼(`Trash2` 아이콘) 디자인을 프로젝트 표준 스타일로 교체하고, 아이템 간 여백 및 호버 효과를 다듬어 세련된 리스트 뷰 완성.

---
## [2026-05-24] 메모/할 일 데이터 통합 및 캘린더 연동 완결

## [2026-05-22] 캐러셀 UI 세부 조정 및 하단 영역 UI/UX 통일화
### 주요 변경 사항
#### 1. 캐러셀 레이아웃 및 브레이크포인트 최적화
- **검색창 중앙 배치**: 검색창(`Search`)을 사이드-바이-사이드 레이아웃에서 분리하여 상단 중앙에 고정 배치함으로써 시각적 안정성 확보.
- **교차 레이아웃 시스템 구현**: 화면 크기에 따라 두 영역의 배치가 지능적으로 변경되도록 개선.
  - **전체 영역(`center-area`)**: `smDT`에서 가로(`flex-row`), `mdDT`에서 세로(`flex-col`) 배치.
  - **상단 영역(`Container`)**: `smDT`에서 세로(`flex-col`), `mdDT`에서 가로(`flex-row`) 배치.
- **여백 및 너비 정밀 조정**: 하단 캐러셀 컨테이너에 `px-5` 패딩을 적용하여 시각적 여백을 확보하고, 고정 너비를 가변 너비(`w-full max-w-186`)로 변경하여 반응형 대응 강화.
- **레이아웃 밀도 최적화**: 1336px(`smDT`) 화면에서 가용 공간을 극대화하기 위해 두 캐러셀 사이의 간격을 `gap-2`로 밀착 조정하고, 상단 영역의 너비를 `smDT:w-115`로 확장.

#### 2. 하단 캐러셀 UI/UX 통일 및 사용성 개선
- **표준 슬라이드 규격 적용**: 모든 슬라이드(달력, 뉴스, 코인, 유튜브)에 공통 글래스모피즘(`.glass`)과 표준 패딩(`p-6`), 높이(`h-full`)를 적용하여 시각적 일관성 달성.
- **타이틀 디자인 표준화**: 모든 슬라이드 상단에 포인트 바(`bg-point`)가 포함된 표준 헤더 구조를 적용하고, '뮤직 라디오' 등 누락된 타이틀 추가.
- **캘린더 슬라이드 개선**: 기본 표시 탭을 '할 일'에서 '메모'로 변경하고, 버튼 순서를 재배치하여 사용자 편의성 증대.
- **페이지네이션 인디케이터 조정**: 슬라이드 콘텐츠 가독성을 위해 도트 위치를 캐러셀 우측 외부(`-right-10`)로 이동.
- **유튜브 슬라이드 'Dark Glass' 최적화**: 기존의 불투명한 검정색 배경을 `bg-black/40`과 `backdrop-blur-md` 기반의 다크 글래스 스타일로 리팩토링하여 전체 디자인 톤과 조화 유도.
- **내부 계층 구조 통일**: 데이터 리스트 영역에 공통적으로 `.glass-sub`를 적용하여 슬라이드 내부에서도 일관된 계층감 유지.

#### 3. 날씨 시스템 및 다크모드 가독성 개선
- **시간 표시(Today) 최적화**: `smDT` 세로 배치 시 공간 확보를 위해 시간 글꼴 크기(`text-5xl`) 및 내부 마진/간격 축소.
- **테마 변수 전수 적용**: 하드코딩된 색상 클래스(`slate-800` 등)를 프로젝트 표준 테마 변수(`text-title`, `text-context`, `bg-point` 등)로 전면 교체하여 다크모드 가독성 완벽 확보.
- **인터랙티브 요소 최적화**: 페이지 인디케이터, 마우스 트래킹 툴팁, 데이터 출처 텍스트 등에 테마별 자동 색상 전환 로직 적용.

---

## [2026-05-21] 메모/할 일 캘린더 연동 (Calendar Integration) 및 Atomic Design 리팩토링
### 주요 변경 사항
#### 1. 캘린더 연동 기반 데이터 모델 확장
- **날짜 필드 도입**: `ListItem` 인터페이스에 `date` 및 `deadline` 필드를 추가하여 모든 메모와 할 일이 날짜 정보를 기반으로 관리되도록 데이터 구조 개선.
- **마감일(Deadline) 시스템**: 할 일(Todo) 추가 시 마감일을 선택할 수 있도록 `ItemInput` 컴포넌트를 확장하고, 마감일 기준 D-1일부터 캘린더에 자동 노출되는 필터링 로직 구현.

#### 2. 캘린더 슬라이드 및 네비게이션 구현
- **인터랙티브 레이아웃**: 하단 캐러셀의 첫 번째 슬라이드에 캘린더 뷰를 배치하여 날짜별 메모/할 일을 한눈에 확인 가능.
- **날짜 네비게이션**: '이전/오늘/다음' 이동 기능을 통해 과거 및 미래의 일정을 손쉽게 탐색할 수 있는 네비게이션 분자 컴포넌트 구현.
- **탭 기반 전환**: 할 일과 메모를 탭 형태로 구분하여 데이터 종류별 필터링 조회 기능 제공.

#### 3. Atomic Design 기반 컴포넌트 리팩토링
- **비즈니스 로직 추출**: 캘린더의 상태 관리 및 필터링 로직을 `useCalendar` 커스텀 훅으로 추출하여 UI 컴포넌트와의 의존성 제거.
- **컴포넌트 모듈화**: `CalendarDateNav`, `CalendarTabs`, `CalendarList` 등 분자(Molecules) 단위로 컴포넌트를 분할하여 코드의 재사용성과 유지보수성 극대화.
- **애니메이션 최적화**: `framer-motion`의 `AnimatePresence`를 활용하여 목록 전환 및 항목 삭제 시 부드러운 시각적 피드백 제공.

---

## [2026-05-20] 유튜브 뮤직 라디오(YouTube Music Radio) 기능 구현
### 주요 변경 사항
#### 1. 유튜브 뮤직 라디오 연동 (YouTube Music Radio Integration)
- **사용자 데이터 연동**: Google YouTube Data API v3를 활용하여 사용자의 '최근 재생 기록'을 동기화하여 나만의 라디오 플레이리스트를 자동 생성.
- **보안 중심 API 아키텍처**: 클라이언트의 API 키 노출을 방지하기 위해 Supabase Edge Function(`youtube-proxy`)을 프록시로 설계하여 보안 강화.
- **라디오 플레이어 전용 로직**: `useRadioPlayer` 커스텀 훅을 통해 플레이리스트 연속 재생, 자동 곡 전환, 볼륨 관리 로직을 구현하여 감상 경험 최적화.
- **상태 관리 고도화**: Redux를 통해 재생 상태, 플레이리스트, 현재 영상 정보를 중앙 집중식으로 안전하게 관리.
- **인증 조건부 렌더링**: 로그인한 사용자에게만 기능이 활성화되도록 보안 정책 적용 및 미로그인 시 로그인 유도 UI 제공.
- **단위 테스트 추가**: 핵심 로직(`useRadioPlayer`)에 대한 테스트 환경을 구축하여 시스템 안정성 확보.
Content from @harness\TASK_CHECKLIST.md:
...
---

## [2026-05-05] 확장 프로그램 환경 최적화 및 코인 데이터 안정성 개선
### 주요 변경 사항
#### 1. 확장 프로그램 빌드 및 아키텍처 안정화
- **빌드 파이프라인 고도화**: Vite 빌드 시 `content.js`가 누락되는 문제와 에셋(아이콘) 경로 오류를 해결하기 위해 `vite.config.ts`의 `rollupOptions` 및 `package.json`의 복사 스크립트를 재설계하여 안정적인 `dist` 생성 체계 구축.
- **아이콘 관리 체계화**: 아이콘 파일 위치를 `public/images/` 내부로 일원화하고 `manifest.json`의 경로 설정을 정규화하여 로딩 에러 해결.
...
- **데이터 유효성 검사**: 업비트 API로부터의 원시 데이터(Raw Data) 처리 시 `split` 에러 등 예상치 못한 데이터 구조에 의한 런타임 에러를 방지하기 위해 방어 코드(Optional Chaining 및 null 체크) 삽입.
- **WebSocket 공유 서비스 구현**: 성능 및 연결 안정성을 위해 매 컴포넌트마다 생성하던 웹소켓 연결을 모듈 수준의 '싱글톤 공유 소켓' 및 '구독자(Subscriber) 패턴'으로 전환.
  - 컴포넌트 재렌더링 시 발생하는 연결 끊김 및 서버 거부 이슈를 근본적으로 차단.
  - 다중 컴포넌트 간 데이터 상태 동기화 비용 최소화.

#### 3. 통합 미디어 플레이어 보안 및 로딩 최적화
- **CSP 정책 강화**: `manifest.json` 및 `index.html`의 Content Security Policy를 수정하여 유튜브 관련 리소스(`iframe`, `script`, `frame`)에 대한 보안 허용 정책을 명시적으로 적용.
- **YouTube IFrame API 리팩토링**: 기존 HTML 내 `iframe` 태그 삽입 방식에서 발생하는 보안 정책 충돌 문제를 해결하기 위해 공식 YouTube IFrame API 기반의 동적 로딩 방식으로 리팩토링하여 플레이어 안정성 확보.
- **플레이어 UI 간소화**: 캐러셀 슬라이드 내 중복된 재생/일시정지 버튼을 제거하고, 유튜브 기본 컨트롤러를 활성화하여 UI 가독성 및 사용자 경험 향상.

#### 4. 다크 모드(Dark Mode) 테마 시스템 구현
- **전역 테마 관리 체계 구축**: Redux `themeSlice` 및 `useTheme` 훅을 통해 테마 상태를 중앙 집중 관리.
- **CSS 변수 기반 디자인 토큰화**: 라이트/다크 모드에 대응하는 색상 토큰 및 글래스모피즘 배경(`glass-bg`), 대시보드 배경 그라데이션(`bg-gradient`)을 변수로 정의하여 자동 전환 적용.
- **테마 토글 UI 추가**: 로그인/프로필 버튼 옆에 `ThemeToggle` 컴포넌트를 배치하여 사용자 편의성 제공.

---

## [2026-05-04] 백그라운드 미디어 제어 확장 (통합 미디어 플레이어 2단계)
...
- 미디어 제어 아키텍처 구축 및 Media Session API 통합, 권한/보안 최적화

## [2026-05-03] 확장 프로그램 아키텍처 재구조화 (Doorframe Remaster v1.0.0 Stable 기반)
- Manifest V3 전환, 샌드박스 팝업 환경 구축, 멀티 엔트리 빌드 설정, 보안 정책 강화

## [2026-04-26] 날씨 시스템 고도화 및 인터랙티브 UI 구축
- 날씨 데이터 엔진 업그레이드, 기온 추이 그래프 및 듀얼 모드 캐러셀, 마우스 트래킹 툴팁 적용
