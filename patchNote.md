# Patch Note - 2026-04-15

## [Fixed] 로그인 무한 대기 및 인증 루프 해결
로컬 스토리지에 토큰이 보존된 상태에서 앱 진입 시, 인증 로직 간의 타이밍 이슈로 인해 화면이 'Loading...' 상태에서 멈추는 현상을 근본적으로 해결했습니다.

### 주요 변경 사항

#### 1. 인증 상태 관리 중앙 집중화 (Redux 도입)
- **`authSlice.ts` 신규 생성**: 유저 정보(`user`)와 인증 로딩 상태(`loading`)를 전역 리덕스 스토어에서 관리하도록 변경했습니다.
- **데이터 일관성 확보**: `useAuth` 훅, `App.tsx`, `ListPersistence` 등 여러 컴포넌트가 각자 세션을 체크하던 방식을 버리고, 하나의 전역 상태를 공유하여 상태 불일치 문제를 제거했습니다.

#### 2. 비동기 병목 및 데드락 제거 (Thunk 최적화)
- **Thunk 내부 `getSession()` 호출 제거**: `loadBookmarksFromStorage`, `loadListFromStorage` 등 Thunk 함수 내부에서 다시 인증 서버를 조회하던 로직을 삭제했습니다.
- **매개변수 기반 로딩**: 이제 Thunk는 외부(App.tsx)에서 전달받은 `userId`를 직접 사용하여 데이터를 조회합니다. 이로써 구글 로그인 리다이렉트 시점에 발생하던 인증 처리 병목을 완전히 해소했습니다.

#### 3. 초기화 프로세스 정렬 (`App.tsx`)
- **인증 흐름 통합**: `App.tsx`에서 초기 세션 확인(`initSession`)과 인증 상태 변경 리스너(`onAuthStateChange`)를 통합 관리합니다.
- **데이터 로드 시점 고정**: 인증이 확인되는 즉시 유저 ID를 Thunk에 전달하여 북마크와 리스트 데이터를 불러오도록 흐름을 최적화했습니다.

#### 4. `useAuth` 훅 경량화 및 안정화
- 복잡한 비동기 초기화 로직을 제거하고, 전역 스토어의 값을 UI에 연결하는 역할로 간소화했습니다.
- 인증 리스너가 어떤 결과(성공/실패/익명)를 반환하든 반드시 로딩 상태를 해제하도록 보장하여 사용자 경험을 개선했습니다.

---

## [2차 패치] 실시간 코인 시세 위젯 및 UI/UX 폴리싱

### 1. 실시간 코인 시세 위젯 추가 및 고도화 (캐러셀 슬라이드 2)
캐러셀 위젯의 두 번째 항목으로 **업비트(Upbit) API** 기반의 실시간 코인 시세 리스트를 도입했습니다.
- **WebSocket 연동**: 업비트 실시간 스트리밍 서버(`wss://api.upbit.com`)를 통해 지연 없는 가격 변동 반영.
- **조작 가시성 개선**: 코인 리스트 영역에 별도의 배경색(`bg-white/25`)과 내부 그림자(`shadow-inner`)를 적용하여, 캐러셀 조작과 독립된 스크롤 영역임을 시각적으로 명확히 했습니다.
- **정밀한 휠 제어**: 리스트 내부 휠 동작 시 리스트 스크롤만, 외부 휠 동작 시 캐러셀 이동이 되도록 이벤트 전파 차단(`stopPropagation`).

### 2. 위젯 레이아웃 일관성 및 시인성 확보
- **위젯 크기 표준화**: 뉴스 브리핑과 코인 리스트의 너비를 `max-w-140`으로 통일하여 슬라이드 전환 시 시각적 흔들림을 방지했습니다.
- **중앙 정렬 최적화**: 모든 캐러셀 위젯에 `mx-auto`를 적용하여 다양한 화면 크기에서도 안정적인 레이아웃을 유지합니다.
- **캐러셀 너비 조정**: 전체 캐러셀 컨테이너의 너비를 `w-165`로 확장하여 위젯 간의 여유 있는 공간을 확보했습니다.

### 3. 디자인 시스템 고도화 및 보안 정책(CSP) 수정
- **전역 유리 질감 최적화**: `global.css`의 `glass`, `glass-input` 클래스들을 더 맑고 투명한 세련된 톤으로 일괄 업데이트했습니다.
- **CSP 정책 업데이트**: 외부 데이터 수신을 위해 업비트 API 및 WebSocket 주소를 보안 정책(`connect-src`)에 추가했습니다.
- **이미지 구조 체계화**: `public/images` 폴더를 생성하여 모든 에셋을 분류하고 코드 내 참조 경로를 일괄 수정했습니다.

### 4. 인증 UI 개선
- **익명 세션 대응**: 비로그인(익명) 상태일 때 프로필 대신 로그인 유도 버튼이 표시되도록 로직을 개선하여 사용자 접근성을 높였습니다.

---
**Next Step:** 세부 기능 고도화 및 사용자 피드백 기반 UI/UX 디테일 개선 예정.