## [2026-09-09] 확장 프로그램 빌드 파이프라인 정규화 및 GitHub Pages 웹 데모 자동 배포 구축

### 주요 변경 사항

#### 1. 크롬 확장 프로그램 `popup.html` 엔트리 정규화 및 번들링 버그 해결
- **팝업 엔트리 프로젝트 루트 재배치**: `public/popup.html`에 위치하여 Vite 정적 복사 시 비번들링 원본이 `dist/popup.html`로 복사되던 구조를 개선. `popup.html`을 프로젝트 루트로 이전하고 `vite.config.ts`의 Rollup input 경로를 재지정하여 번들링된 정상 스크립트가 `dist/popup.html`로 즉각 연결되도록 교정.

#### 2. 크로스 플랫폼 빌드 스크립트 정규화 및 Windows 종속성 제거
- **중복 복사 명령 제거**: `package.json`의 `build` 명령에 포함되어 있던 Windows 전용 명령어(`copy`, `xcopy`)를 제거하고 `vite build`로 일원화. Vite 내장 정적 에셋 복사(`publicDir`) 기능을 온전히 활용하여 Linux(GitHub Actions CI), macOS, Windows 전 환경에서 동일하게 확장 프로그램 빌드가 수행되도록 안정화.

#### 3. 확장 프로그램 우선 기본값 유지 및 GitHub Pages 배포 환경 분리
- **동적 `base` 경로 바인딩**: 평소 로컬 확장 프로그램 빌드 시에는 상대경로(`base: './'`)를 유지하며, GitHub Pages 배포 환경(CI)에서만 `VITE_BASE_PATH` 환경 변수를 주입받아 서브 도메인(`/doorframe/`)으로 빌드되도록 분리 설계.
- **정적 에셋 경로 호환성 확보**: `BookmarkBar`, `Menus`, `iconUploader` 등 코드 내 에셋 참조 경로에 `import.meta.env.BASE_URL`을 적용하여 확장 프로그램과 웹 데모 양쪽에서 리소스 깨짐 없이 안전하게 로드되도록 호환성 부여.

#### 4. 날씨 아이콘 로컬 에셋 참조 경로 상대경로화 및 중복 정리
- **로컬 에셋 기반 경로 수정**: `public/style/wu-icons-style.css` 내의 하드코딩된 절대경로(`/style/icons/`)를 상대경로(`./icons/`)로 보정하여 외부 의존성 없이 순수 로컬 SVG 아이콘 파일들을 오프라인 및 서브패스 환경에서도 무결하게 렌더링되도록 수정. 미사용 중복 파일(`wu-icons-style.min.css`) 정리.

#### 5. 웹 데모 환경 OAuth 리디렉션 서브패스 지원
- **동적 콜백 오리진 지원**: 확장 프로그램 외 일반 웹 브라우저 환경에서 로그인 시, GitHub Pages와 같은 서브패스 URL 구조에서도 정확히 현재 데모 페이지로 리디렉션되도록 `redirectTo` 로직 개선.

#### 6. GitHub Actions 기반 GitHub Pages 자동 배포 파이프라인 구축
- **`.github/workflows/deploy.yml` 추가**: `new-master` 브랜치에 푸시될 때 자동으로 의존성 설치, 웹 데모 빌드, GitHub Pages 배포가 원스톱으로 처리되는 공식 워크플로우 도입.

#### 7. 빌드 캐시 및 불필요한 산출물 Git 관리 대상 정리
- **`.gitignore` 보강**: 번들 분석 결과물(`stats.html`), 에러 로그 디렉토리(`errorLogs/`), Vite 임시 타임스탬프 캐시를 gitignore에 추가하고 원격 추적 해제.

---

## [2026-09-09] 날씨 백엔드 엣지 함수 배포 및 이중 장애 복구(Dual Fallback) 아키텍처 확립 (v1.3.02)
### 주요 변경 사항
#### 1. 날씨 백엔드(Supabase Edge Functions) 프로덕션 배포 완료
- **클라우드 엣지 함수 동기화**: 원격 Supabase Cloud (`thrqbgygmjbnrbgmyxbc`) 프로젝트에 최신 `weather` 엣지 함수를 성공적으로 배포하여 단기예보 및 초단기실황 데이터를 기반으로 24시간 시간별 기온 차트 및 3일 예보 스키마 연동 정상화.

#### 2. VWorld 지오코더 좌표계 보정 및 행정동 변환 버그 해결
- **GPS 좌표계(`crs=EPSG:4326`) 명시**: VWorld API 호출 시 WGS84 좌표계 파라미터를 추가하여, 위경도 좌표가 미터 단위로 오인되어 아프리카 해역으로 처리되던 주소 변환 실패(`알 수 없는 지역`, `126.xx)`) 오류 원천 해결.

#### 3. 기상청 / VWorld 장애 대응 무료 이중 백업(Dual Fallback) 구축
- **1순위 주 엔진**: 대한민국 기상청(KMA) 지상 실측 AWS 데이터 및 국토교통부 VWorld 역지오코딩 우선 호출.
- **2순위 무중단 백업**: 정부 공공데이터 서버 점검, 일일 트래픽 초과, 키 만료 등 장애 발생 시 유럽중기예보센터(ECMWF) 기반의 **Open-Meteo** 기상망과 **BigDataCloud** 글로벌 한글 역지오코더를 자동 호출하여 화면이 `0°`나 `N/A`로 멈추지 않는 24시간 무중단 기상 파이프라인 완성.

#### 4. 프론트엔드 위치 권한 vs 날씨 데이터 상태 분리
- **`[기본 위치]` 오표기 제거**: 사용자가 브라우저 위치 권한을 승인한 경우, 날씨 데이터 수신 상태와 무관하게 `isFallback`이 `false`로 유지되어 `[기본 위치]` 배지가 뜨지 않도록 상태 조건식 정상화.

#### 5. 우측 하단 플로팅 메뉴 제거 및 캐러셀 슬라이드 내 입력창 통합
- **사용자 동선 일원화**: 우측 하단의 플로팅 버튼(`BottomNav/Bottom.tsx`)과 팝오버(`ActionPopover.tsx`)를 제거하고, 캐러셀 첫 번째 슬라이드(`CalendarSlide.tsx`) 내부에 `ItemInput`을 직접 내장.
- **날짜 연동 즉시 등록**: 날짜 네비게이션으로 선택된 날짜(`targetDate`) 및 탭 종류(`todo`/`memo`)에 맞춰 캐러셀 안에서 직접 할 일/메모를 입력하고 즉시 반영되도록 UX 대폭 간소화.

#### 6. 메모/할 일 입력 필드 일체감(glass-sub) 부여 및 UI 폴리싱
- **입력창 배경 일체감 및 가시성 확보**: 입력창(`ItemInput`)에 리스트와 동일한 글래스 서브(`glass-sub`) 스타일을 적용하여 라이트/다크 모드 전체에서 리스트와 완벽한 조화를 이루도록 개선하고, 포커스 시 포인트 링(`focus-within:ring-2`) 및 직관적인 '추가' 액션 버튼을 배치하여 인터랙션 편의성 극대화.
- **Tailwind v4 다크모드 클래스 바인딩 정상화**: `src/styles/global.css`에 `@custom-variant dark (&:where(.dark, .dark *));`를 명시하여 OS 다크모드 설정에 무조건 종속되지 않고 앱의 테마 스위치(`.dark` 클래스)에 맞춰 테마가 정상 전환되도록 교정.
- **마감일 캘린더 아이콘 복원**: 할 일(Todo) 등록 시 마감일 선택 영역에 누락되었던 캘린더 아이콘(`CalendarIcon`)을 복원하고 다크모드 전용 컬러 스킴(`dark:[color-scheme:dark]`) 적용.
- **삭제 아이콘 직관화(Trash2)**: 대각선 꺾쇠 모양으로 어색하게 노출되던 삭제 버튼을 직관적인 휴지통 아이콘(`Trash2`)으로 전면 교체하고, 상시 가시성(`opacity-40`) 및 호버 로즈 피드백(`hover:text-rose-500`)을 부여하여 편리한 삭제 인터랙션 완성.
- **캐러셀 도트 네비게이터 테두리 정돈**: 도트 네비게이터 외곽의 어색한 흑색 테두리(`border-black/50`)를 제거하고 글래스모피즘 표준 스타일(`border-white/20`)로 복원.
- **캐러셀 수직 전환 애니메이션 직관화**: 슬라이드가 순방향(1 ➔ 2 ➔ 3 / 마우스 휠 다운 / 위로 드래그 / 아래 도트 클릭)으로 진행될 때는 전체가 위로 올라가고, 역방향(3 ➔ 2 ➔ 1 / 마우스 휠 업 / 아래로 드래그 / 위 도트 클릭)일 때는 아래로 내려가도록 `framer-motion` variants 및 방향 벡터를 직관적인 상하 슬라이드 모션으로 전면 재설계. 슬라이드 간 공백 딜레이(`mode="wait"`)를 제거하여 자연스러운 물리 연속성 확보.
- **현재 시각/날짜 섹션 좌측 여백 확장**: 데스크톱 뷰에서 시간과 날짜가 카드의 좌측 가장자리에 너무 붙지 않도록 좌측 패딩을 확장(`pl-6 mdDT:pl-8`)하여 시각적 안정감 확보.

#### 7. 캐러셀 슬라이드 정적 임포트(Static Import) 전환 및 모듈 로딩 안정화
- **동적 청크 오류 원천 차단**: 확장 프로그램 환경에서 빌드 갱신 시 구버전 번들과의 해시 불일치로 발생하던 동적 모듈 로딩 에러(`Failed to fetch dynamically imported module`)를 해결하기 위해 뉴스 및 코인 슬라이드를 정적 임포트로 전환하여 단일 메인 번들에 포함. 전환 딜레이 없이 즉각 로드되도록 최적화.

#### 8. 즐겨찾기(북마크) 추가 모달창 불투명도 및 시인성 강화
- **모달 윈도우 고불투명도 적용**: 반투명 글래스(`glass`, 60% 투명도)로 인해 뒷배경 위젯과 텍스트가 비쳐 보이던 현상을 개선하기 위해 모달 본체에 95% 고불투명도 배경(`bg-white/95 dark:bg-slate-900/95`) 및 `shadow-2xl`을 적용하여 시각적 간섭을 원천 차단.
- **백드롭(배경 오버레이) 딤 처리 고도화**: 거의 투명했던 기존 백드롭(`bg-white/5`)을 부드러운 딤 오버레이(`bg-black/40 dark:bg-black/60`)로 변경하여 모달에 시선을 집중시키도록 개선.
- **입력 필드 배경 경량화 및 플레이스홀더 대비 개선**: 입력 필드 배경색이 너무 어두워 글자가 묻히지 않도록 은은하게 연한 배경(`bg-black/[0.02] dark:bg-white/[0.04]`)으로 조정하고, placeholder 텍스트 대비(`placeholder:text-context/60`)를 높여 안내 문구가 한눈에 선명하게 들어오도록 최적화.
- **레이블 연결 및 포커스 인터랙션 정돈**: 하드코딩되어 있던 레이블 `htmlFor` 속성을 각 인풋 `id`와 정확히 연결하고 포커스 링 적용.

#### 9. 비로그인/게스트 북마크 및 메모/할 일 영속성(Persistence) 버그 원천 해결
- **초기 로딩 누락 버그 해결**: `App.tsx`의 세션 초기화(`initSession`) 및 인증 리스너(`onAuthStateChange`)에서 세션이 없는 경우(비로그인 상태) `loadBookmarksFromStorage` 및 `loadListFromStorage` 호출이 누락되어 새로고침 시 저장된 북마크와 메모/할 일이 불러와지지 않고 초기화되던 치명적인 오류 원천 해결.
- **빈 배열 덮어쓰기 방어**: `useFavBar` 및 `ListPersistence`에 이전 상태 참조(`prevBookmarksRef`, `prevListStateRef`)를 적용하여, 초기 마운트 시점이나 사용자 인증 상태 변경 시 저장소를 빈 배열로 덮어쓰지 않도록 안전 가드 강화.

#### 10. 로그인/로그아웃 아이콘 벡터화 및 계정 관리 UI 폴리싱
- **래스터 에셋 의존성 제거 및 벡터 아이콘 도입**: `public/images/login.webp` 래스터 이미지 파일 에셋 호출 방식을 완전히 제거하고, `lucide-react`의 고해상도 벡터 아이콘(`LogIn`, `LogOut`, `User`)으로 전면 교체. 해상도 저하나 깨짐 없이 라이트/다크 테마에 맞춰 선명하게 렌더링되도록 개선.
- **헤더 컨트롤 디자인 및 규격 일체화**: 로그인/사용자 메뉴 버튼 규격을 테마 토글 버튼(`ThemeToggle`)과 동일한 원형 글래스 버튼(`p-2 rounded-full glass-button hover:scale-105 transition-all text-title`) 및 `top-4 right-4` 정렬로 일원화하여 시각적 통일성 완성.
- **사용자 정보 팝오버 및 로그아웃 인터랙션 고도화**:
  - 사용자 프로필 아바타 이미지 부재 시 깨진 이미지 대신 감성적인 `User` 벡터 플레이스홀더 렌더링.
  - 다크 모드에서 부자연스럽던 흰색 사각형 배경(`bg-white`)을 다크 글래스 스타일과 어우러지는 반투명 글래스 컨테이너로 정돈.
  - 로그아웃 버튼에 직관적인 `LogOut` 아이콘을 배치하고 호버 모션 및 반응형 피드백 적용.

#### 11. Chrome 확장 프로그램 전용 Google OAuth 탭 인증 및 다중 콜백 인터셉트 아키텍처 확립
- **Google OAuth 웹뷰 차단(`400 Bad Request`) 원천 해결**: 구글 보안 정책상 크롬 임베디드 웹뷰(`launchWebAuthFlow`)에서 발생하던 `Authorization page could not be loaded` 오류를 극복하기 위해 `chrome.tabs.create` 기반의 브라우저 독립 탭 인증 파이프라인으로 전환.
- **코드 레벨 리디렉션 주소 지정 및 다중 오리진 콜백 가로채기(Interception)**:
  - `supabase.auth.signInWithOAuth` 호출 시 확장 프로그램 고유 엔트리(`chrome.runtime.getURL('index.html')`)를 `redirectTo`로 명시 주입.
  - Supabase 대시보드 설정(Site URL 또는 Redirect URLs)에 따라 `chrome-extension://`, `localhost`, `127.0.0.1`, `*.chromiumapp.org` 중 어떤 콜백 주소로 반환되더라도 `chrome.tabs.onUpdated` 리스너가 인증 페이로드(`code`, `access_token`, `error`)를 코드 레벨에서 가로채 PKCE 세션 교환(`exchangeCodeForSession`) 또는 해시 토큰 설정(`setSession`)을 완벽하게 처리.
  - 인증 성공 즉시 로그인 탭 자동 정리(`chrome.tabs.remove`) 및 사용자 북마크/리스트 동기화 수행.
- **중복 요청 방지 및 우아한 취소(Graceful Cleanup)**:
  - 로그인 진행 중 다중 클릭으로 인한 중복 인증 요청을 차단하기 위해 `isSigningInRef` 락 적용.
  - 사용자가 로그인 도중 탭을 닫을 경우(`chrome.tabs.onRemoved`) 오류 팝업 없이 리스너를 안전하게 해제하도록 처리.

---

## [2026-09-09] 확장 프로그램 프로덕션 배포 및 보안 프록시 아키텍처 확립 (v1.3.01)
### 주요 변경 사항
#### 1. 보안 프록시 아키텍처 구축 및 환경 변수 격리
- **클라이언트 키 노출 차단**: 기상청 Open API, VWorld Geocoder, Google Gemini API 등 일일 호출량과 비용이 연계된 민감한 외부 키를 클라이언트 번들(`dist/`)에서 전면 배제하고 Supabase Edge Functions Secrets로 완전 격리.
- **클라이언트 공개 키 표준화**: 프론트엔드 `.env`에는 RLS 및 Supabase Auth 정책으로 보호되는 `VITE_SUPABASE_URL` 및 `VITE_SUPABASE_ANON_KEY`만 주입되도록 재설계하여 로컬 확장 프로그램 환경에서도 개발자 키가 안전하게 보호되도록 조치.

#### 2. 캐러셀 슬라이드 컴팩트화 및 안정성 제고
- **유튜브 슬라이드 비활성화**: 과도한 권한(Google OAuth scope) 및 유지보수 부담이 큰 유튜브 플레이어 슬라이드(`YoutubePlayerSlide`)를 비활성화(코드는 보존)하여 핵심 3종 슬라이드(캘린더/메모, AI 뉴스, 실시간 코인 시세)로 캐러셀을 컴팩트하게 재편.

#### 3. 단위 테스트 스위트 전수 정상화 (17/17 통과)
- **JSDOM 테스트 환경 보강**: `setup.ts`에 JSDOM 환경용 `localStorage` 모킹을 추가하여 스토리지 의존 컴포넌트 테스트 오류 해소.
- **Redux 구조 변경에 따른 테스트 동기화**: `useAuth`, `useFavBar`, `useRadioPlayer`, `listThunk`, `WeatherContainer`의 Provider 래퍼 및 파라미터 불일치를 최신 상태로 갱신하여 17개 테스트 파일(37개 테스트 항목) 100% 통과 보장.

#### 4. Manifest V3 호환성 및 위치 안전장치(Fallback) 강화
- **위치 권한 및 서울 기본 위치 안전장치**: `public/manifest.json`에 `geolocation` 권한 추가 및 권한 거부 시 서울 기준 날씨로 안전하게 폴백되고 UI에 `[기본 위치]` 안내 배지 노출.
- **Upbit API CORS 차단 해결**: `host_permissions`에 `https://api.upbit.com/*`를 명시적으로 등록하여 코인 시세 조회 시 발생하는 CORS 차단 원천 해소.

#### 5. 크롬 확장 프로그램 프로덕션 빌드 완료
- 원격 Supabase Cloud 백엔드와 연동 검증 및 `npm run build`를 통한 최종 `dist/` 빌드 번들 산출 완료. Chrome 개발자 모드 탑재 준비 완료.

---

## [2026-05-25] UI/UX 폴리싱 (내비게이션 및 팝업 최적화)
### 주요 변경 사항
#### 1. 내비게이션 Popover 최적화
- **너비 일관성 확보**: `Popover` 컴포넌트 내부의 하드코딩된 스타일을 제거하고, 부모 컴포넌트(`ActionPopover`)에서 명시적으로 너비를 제어할 수 있도록 구조 최적화 (기본 너비 500px 적용).
- **코드 정리**: 사용되지 않는 `popoverDiv` CSS 클래스 제거를 통해 코드베이스 유지보수성 향상.

#### 2. 하단 내비게이션 가시성 개선
- **모드별 아이콘 색상 최적화**: 라이트 모드에서는 아이콘 색상을 검은색으로 고정하고, 다크 모드에서는 흰색으로 반전되도록 CSS 필터(`brightness-0`, `dark:invert`, `dark:brightness-100`)를 적용하여 어떤 테마에서도 명확한 아이콘 가시성 확보.

---
## [2026-05-24] 메모/할 일 팝업 UI 디자인 개선
### 주요 변경 사항
#### 1. 팝업 레이아웃 계층 구조화 (Layout Re-architecture)
- **명확한 정보 계층**: 팝업 내부를 `Header`, `Input Area`, `List Area`로 명확히 분리하여 사용자가 정보 흐름을 직관적으로 인지하도록 구조 개선.
- **가시성 향상**: 입력창(`ItemInput`)을 기존 폼 내부에서 벗어나 개별 영역으로 분리하고, 팝업 너비를 400px(데스크탑 환경 최적화)로 확장하여 가독성 증대.

#### 2. 인터랙션 및 피드백 강화
- **포커스 글로우 효과**: 입력창 포커스 시 글래스모피즘 테마에 맞춘 글로우 효과(shadow)를 적용하여 입력 상태 강조.
- **버튼 피드백**: 추가 버튼(`PlusIcon`) 클릭 시 스케일 변환 애니메이션(hover/active)을 적용하여 경쾌한 피드백 구현.
- **마감일 상태 시각화**: 마감일이 설정된 경우 달력 아이콘 색상을 포인트 색상으로 변경하여 상태 식별 용이성 강화.

#### 3. 디자인 시스템 일관성 및 폴리싱
- **글래스모피즘 테마 통일**: 기존 인라인 스타일을 전면 제거하고 Tailwind 유틸리티 클래스 기반의 `glass` 스타일을 적용하여 일관된 시각적 테마 유지.
- **리스트 아이템 디자인**: 체크박스 및 삭제 버튼(`Trash2` 아이콘) 디자인을 프로젝트 표준 스타일로 교체하고, 아이템 간 여백 및 호버 효과를 다듬어 세련된 리스트 뷰 완성.

---
## [2026-05-24] 메모/할 일 데이터 통합 및 캘린더 연동 완결

## [2026-05-22] 캐러셀 UI 세부 조정 및 하단 영역 UI/UX 통일화
### 주요 변경 사항
#### 1. 캐러셀 레이아웃 및 브레이크포인트 최적화
- **검색창 중앙 배치**: 검색창(`Search`)을 사이드-바이-사이드 레이아웃에서 분리하여 상단 중앙에 고정 배치함으로써 시각적 안정성 확보.
- **교차 레이아웃 시스템 구현**: 화면 크기에 따라 두 영역의 배치가 지능적으로 변경되도록 개선.
  - **전체 영역(`center-area`)**: `smDT`에서 가로(`flex-row`), `mdDT`에서 세로(`flex-col`) 배치.
  - **상단 영역(`Container`)**: `smDT`에서 세로(`flex-col`), `mdDT`에서 가로(`flex-row`) 배치.
- **여백 및 너비 정밀 조정**: 하단 캐러셀 컨테이너에 `px-5` 패딩을 적용하여 시각적 여백을 확보하고, 고정 너비를 가변 너비(`w-full max-w-186`)로 변경하여 반응형 대응 강화.
- **레이아웃 밀도 최적화**: 1336px(`smDT`) 화면에서 가용 공간을 극대화하기 위해 두 캐러셀 사이의 간격을 `gap-2`로 밀착 조정하고, 상단 영역의 너비를 `smDT:w-115`로 확장.

#### 2. 하단 캐러셀 UI/UX 통일 및 사용성 개선
- **표준 슬라이드 규격 적용**: 모든 슬라이드(달력, 뉴스, 코인, 유튜브)에 공통 글래스모피즘(`.glass`)과 표준 패딩(`p-6`), 높이(`h-full`)를 적용하여 시각적 일관성 달성.
- **타이틀 디자인 표준화**: 모든 슬라이드 상단에 포인트 바(`bg-point`)가 포함된 표준 헤더 구조를 적용하고, '뮤직 라디오' 등 누락된 타이틀 추가.
- **캘린더 슬라이드 개선**: 기본 표시 탭을 '할 일'에서 '메모'로 변경하고, 버튼 순서를 재배치하여 사용자 편의성 증대.
- **페이지네이션 인디케이터 조정**: 슬라이드 콘텐츠 가독성을 위해 도트 위치를 캐러셀 우측 외부(`-right-10`)로 이동.
- **유튜브 슬라이드 'Dark Glass' 최적화**: 기존의 불투명한 검정색 배경을 `bg-black/40`과 `backdrop-blur-md` 기반의 다크 글래스 스타일로 리팩토링하여 전체 디자인 톤과 조화 유도.
- **내부 계층 구조 통일**: 데이터 리스트 영역에 공통적으로 `.glass-sub`를 적용하여 슬라이드 내부에서도 일관된 계층감 유지.

#### 3. 날씨 시스템 및 다크모드 가독성 개선
- **시간 표시(Today) 최적화**: `smDT` 세로 배치 시 공간 확보를 위해 시간 글꼴 크기(`text-5xl`) 및 내부 마진/간격 축소.
- **테마 변수 전수 적용**: 하드코딩된 색상 클래스(`slate-800` 등)를 프로젝트 표준 테마 변수(`text-title`, `text-context`, `bg-point` 등)로 전면 교체하여 다크모드 가독성 완벽 확보.
- **인터랙티브 요소 최적화**: 페이지 인디케이터, 마우스 트래킹 툴팁, 데이터 출처 텍스트 등에 테마별 자동 색상 전환 로직 적용.

---

## [2026-05-21] 메모/할 일 캘린더 연동 (Calendar Integration) 및 Atomic Design 리팩토링
### 주요 변경 사항
#### 1. 캘린더 연동 기반 데이터 모델 확장
- **날짜 필드 도입**: `ListItem` 인터페이스에 `date` 및 `deadline` 필드를 추가하여 모든 메모와 할 일이 날짜 정보를 기반으로 관리되도록 데이터 구조 개선.
- **마감일(Deadline) 시스템**: 할 일(Todo) 추가 시 마감일을 선택할 수 있도록 `ItemInput` 컴포넌트를 확장하고, 마감일 기준 D-1일부터 캘린더에 자동 노출되는 필터링 로직 구현.

#### 2. 캘린더 슬라이드 및 네비게이션 구현
- **인터랙티브 레이아웃**: 하단 캐러셀의 첫 번째 슬라이드에 캘린더 뷰를 배치하여 날짜별 메모/할 일을 한눈에 확인 가능.
- **날짜 네비게이션**: '이전/오늘/다음' 이동 기능을 통해 과거 및 미래의 일정을 손쉽게 탐색할 수 있는 네비게이션 분자 컴포넌트 구현.
- **탭 기반 전환**: 할 일과 메모를 탭 형태로 구분하여 데이터 종류별 필터링 조회 기능 제공.

#### 3. Atomic Design 기반 컴포넌트 리팩토링
- **비즈니스 로직 추출**: 캘린더의 상태 관리 및 필터링 로직을 `useCalendar` 커스텀 훅으로 추출하여 UI 컴포넌트와의 의존성 제거.
- **컴포넌트 모듈화**: `CalendarDateNav`, `CalendarTabs`, `CalendarList` 등 분자(Molecules) 단위로 컴포넌트를 분할하여 코드의 재사용성과 유지보수성 극대화.
- **애니메이션 최적화**: `framer-motion`의 `AnimatePresence`를 활용하여 목록 전환 및 항목 삭제 시 부드러운 시각적 피드백 제공.

---

## [2026-05-20] 유튜브 뮤직 라디오(YouTube Music Radio) 기능 구현
### 주요 변경 사항
#### 1. 유튜브 뮤직 라디오 연동 (YouTube Music Radio Integration)
- **사용자 데이터 연동**: Google YouTube Data API v3를 활용하여 사용자의 '최근 재생 기록'을 동기화하여 나만의 라디오 플레이리스트를 자동 생성.
- **보안 중심 API 아키텍처**: 클라이언트의 API 키 노출을 방지하기 위해 Supabase Edge Function(`youtube-proxy`)을 프록시로 설계하여 보안 강화.
- **라디오 플레이어 전용 로직**: `useRadioPlayer` 커스텀 훅을 통해 플레이리스트 연속 재생, 자동 곡 전환, 볼륨 관리 로직을 구현하여 감상 경험 최적화.
- **상태 관리 고도화**: Redux를 통해 재생 상태, 플레이리스트, 현재 영상 정보를 중앙 집중식으로 안전하게 관리.
- **인증 조건부 렌더링**: 로그인한 사용자에게만 기능이 활성화되도록 보안 정책 적용 및 미로그인 시 로그인 유도 UI 제공.
- **단위 테스트 추가**: 핵심 로직(`useRadioPlayer`)에 대한 테스트 환경을 구축하여 시스템 안정성 확보.
Content from @harness\TASK_CHECKLIST.md:
...
---

## [2026-05-05] 확장 프로그램 환경 최적화 및 코인 데이터 안정성 개선
### 주요 변경 사항
#### 1. 확장 프로그램 빌드 및 아키텍처 안정화
- **빌드 파이프라인 고도화**: Vite 빌드 시 `content.js`가 누락되는 문제와 에셋(아이콘) 경로 오류를 해결하기 위해 `vite.config.ts`의 `rollupOptions` 및 `package.json`의 복사 스크립트를 재설계하여 안정적인 `dist` 생성 체계 구축.
- **아이콘 관리 체계화**: 아이콘 파일 위치를 `public/images/` 내부로 일원화하고 `manifest.json`의 경로 설정을 정규화하여 로딩 에러 해결.
...
- **데이터 유효성 검사**: 업비트 API로부터의 원시 데이터(Raw Data) 처리 시 `split` 에러 등 예상치 못한 데이터 구조에 의한 런타임 에러를 방지하기 위해 방어 코드(Optional Chaining 및 null 체크) 삽입.
- **WebSocket 공유 서비스 구현**: 성능 및 연결 안정성을 위해 매 컴포넌트마다 생성하던 웹소켓 연결을 모듈 수준의 '싱글톤 공유 소켓' 및 '구독자(Subscriber) 패턴'으로 전환.
  - 컴포넌트 재렌더링 시 발생하는 연결 끊김 및 서버 거부 이슈를 근본적으로 차단.
  - 다중 컴포넌트 간 데이터 상태 동기화 비용 최소화.

#### 3. 통합 미디어 플레이어 보안 및 로딩 최적화
- **CSP 정책 강화**: `manifest.json` 및 `index.html`의 Content Security Policy를 수정하여 유튜브 관련 리소스(`iframe`, `script`, `frame`)에 대한 보안 허용 정책을 명시적으로 적용.
- **YouTube IFrame API 리팩토링**: 기존 HTML 내 `iframe` 태그 삽입 방식에서 발생하는 보안 정책 충돌 문제를 해결하기 위해 공식 YouTube IFrame API 기반의 동적 로딩 방식으로 리팩토링하여 플레이어 안정성 확보.
- **플레이어 UI 간소화**: 캐러셀 슬라이드 내 중복된 재생/일시정지 버튼을 제거하고, 유튜브 기본 컨트롤러를 활성화하여 UI 가독성 및 사용자 경험 향상.

#### 4. 다크 모드(Dark Mode) 테마 시스템 구현
- **전역 테마 관리 체계 구축**: Redux `themeSlice` 및 `useTheme` 훅을 통해 테마 상태를 중앙 집중 관리.
- **CSS 변수 기반 디자인 토큰화**: 라이트/다크 모드에 대응하는 색상 토큰 및 글래스모피즘 배경(`glass-bg`), 대시보드 배경 그라데이션(`bg-gradient`)을 변수로 정의하여 자동 전환 적용.
- **테마 토글 UI 추가**: 로그인/프로필 버튼 옆에 `ThemeToggle` 컴포넌트를 배치하여 사용자 편의성 제공.

---

## [2026-05-04] 백그라운드 미디어 제어 확장 (통합 미디어 플레이어 2단계)
...
- 미디어 제어 아키텍처 구축 및 Media Session API 통합, 권한/보안 최적화

## [2026-05-03] 확장 프로그램 아키텍처 재구조화 (Doorframe Remaster v1.0.0 Stable 기반)
- Manifest V3 전환, 샌드박스 팝업 환경 구축, 멀티 엔트리 빌드 설정, 보안 정책 강화

## [2026-04-26] 날씨 시스템 고도화 및 인터랙티브 UI 구축
- 날씨 데이터 엔진 업그레이드, 기온 추이 그래프 및 듀얼 모드 캐러셀, 마우스 트래킹 툴팁 적용
