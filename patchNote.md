# Patch Note - 2026-04-15

## [Fixed] 로그인 무한 대기 및 인증 루프 해결
로컬 스토리지에 토큰이 보존된 상태에서 앱 진입 시, 인증 로직 간의 타이밍 이슈로 인해 화면이 'Loading...' 상태에서 멈추는 현상을 근본적으로 해결했습니다.

### 주요 변경 사항

#### 1. 인증 상태 관리 중앙 집중화 (Redux 도입)
- **`authSlice.ts` 신규 생성**: 유저 정보(`user`)와 인증 로딩 상태(`loading`)를 전역 리덕스 스토어에서 관리하도록 변경했습니다.
- **데이터 일관성 확보**: `useAuth` 훅, `App.tsx`, `ListPersistence` 등 여러 컴포넌트가 각자 세션을 체크하던 방식을 버리고, 하나의 전역 상태를 공유하여 상태 불일치 문제를 제거했습니다.

#### 2. 비동기 병목 및 데드락 제거 (Thunk 최적화)
- **Thunk 내부 `getSession()` 호출 제거**: `loadBookmarksFromStorage`, `loadListFromStorage` 등 Thunk 함수 내부에서 다시 인증 서버를 조회하던 로직을 삭제했습니다.
- **매개변수 기반 로딩**: 이제 Thunk는 외부(App.tsx)에서 전달받은 `userId`를 직접 사용하여 데이터를 조회합니다. 이로써 구글 로그인 리다이렉트 시점에 발생하던 인증 처리 병목을 완전히 해소했습니다.

#### 3. 초기화 프로세스 정렬 (`App.tsx`)
- **인증 흐름 통합**: `App.tsx`에서 초기 세션 확인(`initSession`)과 인증 상태 변경 리스너(`onAuthStateChange`)를 통합 관리합니다.
- **데이터 로드 시점 고정**: 인증이 확인되는 즉시 유저 ID를 Thunk에 전달하여 북마크와 리스트 데이터를 불러오도록 흐름을 최적화했습니다.

#### 4. `useAuth` 훅 경량화 및 안정화
- 복잡한 비동기 초기화 로직을 제거하고, 전역 스토어의 값을 UI에 연결하는 역할로 간소화했습니다.
- 인증 리스너가 어떤 결과(성공/실패/익명)를 반환하든 반드시 로딩 상태를 해제하도록 보장하여 사용자 경험을 개선했습니다.

---
**Next Step:** 모든 핵심 기술적 이슈가 해결되었으므로, 향후 UI/UX 폴리싱 및 추가 기능 구현에 집중할 예정입니다.
